// Compatibility shim for legacy toast context import paths
export { useToast } from "../features/shared/hooks/useToast";
export type {
  Toast,
  ToastContextType,
} from "../features/shared/hooks/useToast";
